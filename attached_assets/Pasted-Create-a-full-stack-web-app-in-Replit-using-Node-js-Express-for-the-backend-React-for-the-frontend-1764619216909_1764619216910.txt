Create a full-stack web app in Replit using Node.js, Express for the backend, React for the frontend, and integrate AI capabilities via OpenAI or Anthropic SDKs (use environment variables for API keys). The app is called “Personal AI Agent” and acts as an autonomous personal assistant with these core features:
1.  Autonomous App Creation: The agent takes user requirements (e.g., “Build a simple HR compliance tracker app”) via a chat interface. It independently plans steps, generates code (e.g., in Python, JS, or HTML), handles third-party integrations (e.g., connect to APIs like GitHub via Octokit, Notion, or payment gateways using libraries like Axios), tests the code, and deploys it (e.g., push to a new Replit project or GitHub repo). Use a framework like LangChain or CrewAI for agentic behavior—implement multi-step reasoning where the agent breaks down tasks, executes code in a sandbox (use Node’s child_process for safe execution), and suggests alternatives (e.g., “Use no-code tools like Bubble.io instead for faster prototyping”).
2.  UAE Labor Law Expertise: Embed knowledge of UAE Federal Decree-Law No. 33 of 2021 (and 2025 updates) for HR tasks. Use Retrieval-Augmented Generation (RAG): Store law documents as vectors in a simple in-memory database (e.g., via Pinecone.js or FAISS alternative). For queries like “Advise on employee termination,” retrieve relevant articles (e.g., notice periods, gratuity calculations), provide solutions/recommendations, and perform tasks (e.g., generate compliant contract templates as PDFs using pdf-lib). Ensure compliance with PDPL for data handling.
3.  Proactive Recommendations and Bigger Picture Thinking: For any task, the agent always analyzes for better alternatives (e.g., if asked to build an app, suggest integrating with existing tools like Microsoft Power Automate if it’s HR-related). Use chain-of-thought prompting in the AI model: Start with “Analyze request, identify optimizations/alternatives, then execute.” Make it a true personal assistant by supporting tasks like scheduling (integrate Google Calendar API), reminders, or data analysis.
Overall Structure:
•  Backend (Express): API endpoints for /chat (handle messages, mode: ‘dev’ for app creation or ‘hr’ for labor law), /models (list AI models like GPT-4o, Claude). Use Drizzle ORM with Neon Postgres for user auth (username/password hashing with bcrypt). Integrate AI providers (OpenAI, Anthropic) for responses.
•  Frontend (React): Chat UI with Wouter routing, Tanstack Query for API fetching, Framer Motion for animations, Tailwind CSS. Include mode selector (Dev/HR) and model picker.
•  Agentic Logic: Use LangChain.js to create an agent with tools: code generation/execution, API callers for integrations, RAG for laws. For app creation, generate files and deploy via Replit’s API or Git.
•  PWA Support: Add manifest.json for installable app, service worker for offline caching.
•  Dependencies: Add langchain, @langchain/openai, @langchain/anthropic, pdf-lib, bcrypt, axios, octokit, notion-client.
•  Deployment: Make it run on Replit with a public URL. Include error handling and logging.
Generate the complete code structure, including package.json, tsconfig.json, database schema, and main files. Use TypeScript for type safety. Ensure it’s modular and extensible. Finally, provide instructions on how to set up API keys in Replit secrets.